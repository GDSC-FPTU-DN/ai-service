version: '3.8'

services:
  api:
    build: .
    container_name: "gdsc-ai-service-container"
    image: "gdscfptu/ai-service"
    ports:
      - "8000:80"
    volumes:
      - .:/app
      - /app/env
    env_file:
      - ./.env